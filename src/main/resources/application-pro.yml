spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://tooffdb.c8ybqxqdchai.ap-northeast-1.rds.amazonaws.com:3306/tooffdb?useUnicode=true&characterEncoding=utf-8&useSSL=true
    username: root
    password: Zdh19850612
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
      exclude: templates/**, static/**
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  aop:
    proxy-target-class: true
  redis:
    host: 35.76.43.135
    port: 6379
    password: tooff.

logging:
  level:
    jp.co.amazon2off: debug

#上传位置
uploadDir: /Tmp_Image/

mybatis-plus:
  global-config:
    db-config:
      id-type: auto
    banner: false